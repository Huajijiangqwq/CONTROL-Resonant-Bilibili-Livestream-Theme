# 安全与本地数据

Beta 1 的服务只监听 `127.0.0.1`，用于同一台电脑上的浏览器和 OBS。不要通过公网反向代理、端口转发或 LAN 暴露它们。来自其他本机进程的访问不视为隔离的多用户安全边界。

扫码取得的 SESSDATA 使用 Windows DPAPI CurrentUser 加密，单独保存在本机用户数据目录；手动输入的 SESSDATA 与 OBS 连接密码仅用于本地接入内存。登录凭据不进入主题 JSON、撤销历史或导出包。点击“清除已保存登录”会删除本机凭据并断开网页接入；这不是 Bilibili 账户全设备注销。主题与导入素材本身可能包含姓名、图片及其他私人内容，分享前请检查。自动导出工具会排除服务配置、绑定、草稿与日志，手动上传整个工作目录不享有同样的保障。

如果仓库开启了 GitHub 私密漏洞报告，请使用 Security → Report a vulnerability；否则先发一个不包含利用细节或秘密的 Issue，请维护者提供私密沟通渠道。不要把凭据、完整 OBS 配置或直播间日志直接贴到公开 Issue。

目前只维护最新 Beta。尚未进行独立安全审计。浏览器源与 Node.js / OBS 本身也需要保持更新。
