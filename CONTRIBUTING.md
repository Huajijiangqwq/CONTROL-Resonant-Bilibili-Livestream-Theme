# 参与项目

感谢帮助改进主题。当前以完善现有主题和编辑器为主；

1. 用 Node.js 24+ 启动本地副本；同时开多个副本时使用不同端口组。
2. 修改前导出自己的主题文件。不要把个人主题、OBS 绑定、凭据或录制内容加入提交。
3. 提交前运行 `npm run check` 和 `npm test`；涉及 Windows 音频时另验证首次编译、设备切换和静音。
4. 视觉修改应同时检查静帧与连续运动。请提供短录屏，说明布局、组件等级、窗口尺寸、帧率和改动理由；不要仅靠截图证明动画效果一致。
5. 涉及 OBS 的修改先用模拟测试，再在隔离场景验证，不要改变正在直播的节目场景。

PR 尽量围绕一个具体问题。新增依赖必须说明来源、版本、许可，并更新第三方清单。不得提交无再分发授权的游戏素材、音乐、商用字体或账号信息。

JS / CSS 使用 Prettier 3.6.2 整理。需要格式化时可自行运行 `npx prettier@3.6.2 --write "app/**/*.js" "app/**/*.css" "scripts/**/*.js" "tests/**/*.js"`；格式化工具仅用于开发，不是启动依赖。第三方 fft.js 保持上游格式。

项目原创贡献按 MIT 提供；第三方代码遵循原许可。本仓库不要求额外 CLA。
